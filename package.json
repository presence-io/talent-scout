{
  "name": "talent-scout",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint packages/",
    "lint:fix": "eslint packages/ --fix",
    "format:check": "prettier --check 'packages/**/*.{ts,tsx,json,md}'",
    "format": "prettier --write 'packages/**/*.{ts,tsx,json,md}'",
    "typecheck": "tsc --build",
    "check": "pnpm typecheck && pnpm lint && pnpm format:check && pnpm test:coverage",
    "pipeline": "pnpm --filter @talent-scout/skills run skill pipeline",
    "cron:sync": "pnpm --filter @talent-scout/skills run skill cron sync",
    "preinstall": "node scripts/check-deps.cjs"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "@vitest/coverage-v8": "^3.1.1",
    "eslint": "^9.23.0",
    "eslint-config-prettier": "^10.1.1",
    "prettier": "^3.5.3",
    "prettier-plugin-astro": "^0.14.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "typescript": "~5.8.2",
    "typescript-eslint": "^8.28.0",
    "vitest": "^3.1.1"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  },
  "engines": {
    "node": ">=22"
  }
}
